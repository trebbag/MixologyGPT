{
  "annotations": { "list": [] },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum by (domain) (crawler_domain_retryable_jobs)",
          "legendFormat": "{{domain}}",
          "refId": "A"
        }
      ],
      "title": "Retry Queue By Domain",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "max by (domain) (crawler_domain_failure_rate)",
          "legendFormat": "{{domain}}",
          "refId": "A"
        }
      ],
      "title": "Failure Rate By Domain",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "max by (domain) (crawler_domain_parser_fallback_rate)",
          "legendFormat": "{{domain}}",
          "refId": "A"
        }
      ],
      "title": "Parser Fallback Rate By Domain",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum by (domain, failure_class) (rate(crawler_parse_failures_total[15m]))",
          "legendFormat": "{{domain}}/{{failure_class}}",
          "refId": "A"
        }
      ],
      "title": "Parse Failure Classes (rate)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 },
      "id": 5,
      "targets": [
        {
          "expr": "sum by (domain, status) (rate(crawler_jobs_total[15m]))",
          "legendFormat": "{{domain}}/{{status}}",
          "refId": "A"
        }
      ],
      "title": "Crawler Job Outcomes (rate)",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["bartenderai", "crawler"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "BartenderAI Crawler Ops",
  "version": 1
}
